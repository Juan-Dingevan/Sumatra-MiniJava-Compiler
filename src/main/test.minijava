public static void main(String args) {
try {
            SourceManager sm = new SourceManagerImpl(); //this is a source manager, it manages the sources!
            sm.open("src/test.minijava");

            char an_underscore_char = '_'
            char a = 'thisIsntHowThisGoes'
            char b = 'c
            char c = '\'

            /*
                Now we go into a sort of loop thing.
                Idk what this does, actually, but it
                sure does look technological!!
            */

            float aFloatForNoReason = 19.1220011200112001;
            int x = 3;

            String s = /*oh god a multi-line single-line comment*/ "glad that works man!";

            String s = "oops I forgor to close
            var ejemplo = v1 + # + close

            while(true) {
                char c = sm.getNextChar();
                System.out.println("Line: " + sm.getLineNumber() + " Index: " + sm.getLineIndex() + " Read: " + c + "(" + (int) c + ")");

                if(CharacterIdentifier.isEOF(c))
                    break;
            }

            sm.close();
        } catch(Exception ex) {
            ex.printStackTrace();
        }

    }
}

//end of file comment. does this work i wonder. why yes, yes it does
